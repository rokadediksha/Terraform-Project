# Terraform state & secrets
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfplan

# tfvars may contain passwords — keep out of git
terraform.tfvars

# OS noise
.DS_Store
Thumbs.db
